# Containers

Container image files (`*.sif`) are intentionally excluded from Git because
they are large binary artifacts. Rebuild or download them by following the
project installation documentation.
