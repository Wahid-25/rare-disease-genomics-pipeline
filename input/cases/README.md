# Prepared cases

Prepared case directories are generated locally by the pipeline and are not
tracked in Git. Validation inputs are maintained separately under
`validation/universal_pipeline_testing/inputs/`.
